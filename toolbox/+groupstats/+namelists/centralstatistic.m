function names = centralstatistic()
   %CENTRALSTATISTIC Valid values for a method option that summarizes a group.
   %
   %  names = groupstats.namelists.centralstatistic()
   %
   % Description
   %  Returns the statistics a chart computes for each group before plotting
   %  it as one value.
   %
   % See also: groupstats.barchartcats

   names = [
      "mean"
      "median"
      ];
end
