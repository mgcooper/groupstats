function names = sortdatavar()
   %SORTDATAVAR Data arguments a sort order can read.
   %
   %  names = groupstats.namelists.sortdatavar()
   %
   % Description
   %  Returns the names of the data arguments that a SortVar option chooses
   %  between. Each value names an argument of the calling function.
   %
   % See also: groupstats.scatter

   names = [
      "xdatavar"
      "ydatavar"
      ];
end
