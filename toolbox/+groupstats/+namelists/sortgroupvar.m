function names = sortgroupvar()
   %SORTGROUPVAR Grouping arguments a sort order can follow.
   %
   %  names = groupstats.namelists.sortgroupvar()
   %
   % Description
   %  Returns the names of the grouping arguments that a SortGroup option
   %  chooses between. Each value names an argument of the calling function.
   %
   % See also: groupstats.scatter

   names = [
      "cgroupvar"
      "sgroupvar"
      ];
end
